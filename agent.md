# Agent Instructions

- Before starting any task, update `TASKS_STATUS.txt` with current objectives and timestamp.
- When tasks are completed (or before stopping), update `TASKS_STATUS.txt` with progress and completion notes.
